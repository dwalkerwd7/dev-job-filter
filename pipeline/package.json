{
  "name": "smb-job-scraper",
  "version": "1.0.0",
  "description": "",
  "license": "ISC",
  "author": "",
  "type": "commonjs",
  "main": "pipeline.js",
  "scripts": {
    "scrape": "varlock run -- node pipeline.js --no-filtering --no-aligning",
    "filter": "varlock run -- node pipeline.js --no-scraping --no-aligning",
    "align": "varlock run -- node pipeline.js --no-scraping --no-filtering",
    "pipeline": "varlock run -- node pipeline.js"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.82.0",
    "mongoose": "^9.4.1",
    "playwright": "^1.59.1",
    "varlock": "^0.7.1"
  }
}
